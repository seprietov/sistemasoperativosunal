El metodo "os.chdir(path)" solicita como argumento un str con la ruta a la cual se cambiará el espacio de tranajo
El metodo "os.environ" mapea las varibles del entorno retornando un diccionario de estas
El metodo os.fsencode(filename) codifica el nombre del archivo retornando bytes. El proceso inverso lo realiza os.fsdecode(filename)
El metodo os.get_exec_path() retorn una lista de directorios donde se puede buscar un ejecutable por nombre
El metodo "os.getcwd()" retorna un string con la ruta al directorio de trabajo actual